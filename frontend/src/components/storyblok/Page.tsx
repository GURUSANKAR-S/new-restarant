import { ISbRteField, StoryblokComponent } from '@storyblok/react';&#10;&#10;interface PageProps {&#10;  blok: {&#10;    body: StoryblokComponent[];&#10;  };&#10;};&#10;&#10;export default function Page({ blok }: PageProps) {&#10;  return (&#10;    <>&#10;      {blok.body?.map((nestedBlok: StoryblokComponent) => (&#10;        <React.Fragment key={nestedBlok._uid}>&#10;          {nestedBlok.component === 'menu_block' ? (&#10;            // Delegate to child component&#10;            ''&#10;          ) : null}&#10;        </React.Fragment>&#10;      ))}&#10;    </>&#10;  );&#10;}"

